const express = require('express');
const router = express.Router();
const pool = require('../db');

// модуль для внутренних процессов с данными (загрузить пост, лайки, страницу)

module.exports = router;
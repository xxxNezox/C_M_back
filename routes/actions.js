const express = require('express');
const router = express.Router();
const pool = require('../db');

// модуль для активных действий пользователя (поставить лайк, подписаться, создать пост)

module.exports = router;